import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.apollo.hsetoolkit',
  appName: 'دستیار خطرات محیط کار',
  webDir: 'www'
};

export default config;
